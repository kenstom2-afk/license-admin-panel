services:
  - type: web
    name: license-admin-panel-fixed
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: SECRET_KEY
        generateValue: true
    autoDeploy: true
